# Phase 0/1 — connectors + storage
httpx>=0.27
beautifulsoup4>=4.12
lxml>=5.0
python-dotenv==1.0.1
SQLAlchemy>=2.0

# Phase 2 — tailoring + document export
PyYAML>=6.0
python-docx>=1.1
anthropic>=0.34
openai>=1.40

# Phase 3 — local API
fastapi>=0.110
uvicorn>=0.29
